[project]
name = "book-library"
version = "0.1.0"
description = "Physical book library system using OOP in Python"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest", "ruff"]

[tool.pytest.ini_options]
testpaths = ["tests"]
